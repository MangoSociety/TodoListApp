package com.example.kotlin_practice.exam.medium

// Реализовать делегат для отложенной инициализации с таймаутом
class TimeoutLazy<T>(
    private val timeoutMs: Long,
    private val initializer: () -> T
) {
    // Реализовать делегат, который:
    // 1. Инициализирует значение только при первом обращении
    // 2. Если инициализация занимает больше timeoutMs, использует значение по умолчанию
    // 3. Поддерживает потокобезопасность
    // 4. Предоставляет метод для сброса значения
}