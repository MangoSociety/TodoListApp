package com.example.kotlin_practice.exam.easy

data class Person(
    val name: String,
    val age: Int
)

fun updateAge(person: Person, newAge: Int) {
    // Создать копию person с новым возрастом
}